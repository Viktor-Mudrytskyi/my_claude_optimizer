{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; if [[ \"$f\" == *.md ]]; then prettier --write \"$f\"; fi; } 2>/dev/null || true",
            "statusMessage": "Formatting markdown..."
          }
        ]
      }
    ]
  }
}