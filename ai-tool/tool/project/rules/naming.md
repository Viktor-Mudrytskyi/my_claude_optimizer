# Naming

- File names use `snake_case` (e.g. `main.dart`).
- Class names use `PascalCase` (e.g. `MainApp`).
- Package name uses `snake_case` (`flutter_application`).
- Follows standard Dart naming conventions.
