# Linting

- Uses `package:flutter_lints/flutter.yaml` as the base rule set (via `include:` in `analysis_options.yaml`).
- No custom rules or overrides are defined — all defaults from `flutter_lints ^6.0.0` apply.
